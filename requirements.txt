z3-solver>=4.12
# xahc-watch (the fourth leg): address codec + live ledger transport. Offline tests/replay
# import neither (websockets is lazy-loaded), so the prover core needs only z3.
xrpl-py>=2.5
websockets>=12.0
